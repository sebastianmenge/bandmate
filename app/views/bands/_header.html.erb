<div id="top_menu_1">
  <div class="logo">
    <%= image_tag 'logo_gelb_smaller.png', :class => "logo" %>
  </div>
  <nav>
    <ul>
      <li><h5>Bands</h5></li>
      <li><h5>Proberaum</h5></li>
    </ul>
  </nav>
  <div class="logged_user">
    <div class="logged_in_as">
      <p>Logged in as:</p>
    </div>  
  <% if user_signed_in? %>
    <%= link_to profile_path do %>
      <%= current_user.username.split(" ").collect { |p| p.capitalize }.join(" ") %>
    <% end %>
  <% end %>
  </div>
  <div class="logout">
    <%= link_to image_tag('logout.png', :onmouseover =>'logout_over.png'), destroy_user_session_path, :method => :delete  %>
  </div>
</div>