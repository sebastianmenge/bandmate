<%= title "#{current_user.username}"%>

<div class="main_container round">
  <div>
    <% if current_user.avatar? %>
      <%= image_tag current_user.avatar.url(:original), :class => "profile_image" %>
    <% else %>
      <%= image_tag 'no_prof_img.png', :class => "profile_image" %>
    <% end %>
    </div>
  <div class="username"><%= current_user.username.split(" ").collect { |p| p.capitalize }.join(" ") %></div>
  <p><%= link_to "Edit Profile", edit_user_registration_path %></p>

  <div class="add_band">
    <p>Du hast noch keine Bands oder Projekte erstellt.<br>
      Erstelle jetzt eine Band/s</p>
      <div><%=%>  </div>
  </div>
  <div class="add_room">
    <p>Noch keine Proberäume vorhanden.<br>
      Erstelle einen neuen Proberaum und lade Freunde ein</p>
  </div>
</div>
